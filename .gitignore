.DS_Store
**/.DS_Store
Thumbs.db
node_modules/

# ローカル開発用（プレビューサーバー設定）。skills/ だけは運用手順なので追跡する
.claude/*
!.claude/skills/
.codex/
build/.cache/

# Playwright の実行結果（生成物）
test-results/
playwright-report/
dist/

# KPI の入力（管理画面から出した集計 CSV）。**リポジトリにも公開物にも入れない**
private/

# OGP 画像（生成物）。**コミットしない**（1,700 枚を超えて履歴が膨らむため。2026-09-11）。
# 公開物は .github/workflows/pages.yml が CI で作る。台帳 build/data/ogp-hashes.json はコミットする
assets/ogp/
assets/ogp-*.png
assets/ogp.png
