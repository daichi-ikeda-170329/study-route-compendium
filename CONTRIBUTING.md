# 変更するときの約束

このサイトが最優先するのは**受験生への公益**です。順位は次のとおりで、
上を下のために犠牲にしません。

1. 受験生への公益 — 誤った教材・科目・学習期間・大学情報へ誘導しない
2. 正確性と検証可能性 — 事実・編集部の推定・未確認を分ける
3. 検索からの発見性
4. 継続して運営できること（アフィリエイトと広告）
5. 保守性

## 変更の単位

内容ごとに分けます。誤情報の修正・アクセシビリティ・データ構造・SEO・CI・
ドキュメントを 1 つのコミットに混ぜません。整形だけの変更と挙動が変わる変更も
混ぜません。切り戻せる単位にしておくと、あとで困りません。

## データの出典

- 一次情報を優先します。**出版社の公式 → 書誌データベース（openBD・国立国会図書館）
  → 大学の公式発表**の順です。
- EC サイトのレビュー、個人ブログ、まとめサイトは**事実確認の根拠にしません**。
  自動生成された商品説明も採りません。
- **推測で埋めません。** 分からない値は空文字や 0 ではなく `null` にし、
  確認状態は `unverified` のままにします。
- 詳しくは [docs/data-verification.md](docs/data-verification.md)。

## 確認状態

`build/data/verification.json` が持ちます。**出典 URL と確認日の両方が無いものを
`verified` にしません。** `physicalReview: true` は人が実物を確認したときだけで、
API での照合では立てません。

```bash
npm run fetch:verification   # openBD と照合して確認状態を作り直す
npm run report:quality       # docs/data-quality.md に件数を出す
```

## 生成物

生成物はコミットします（GitHub Pages が静的ファイルを配信するため）。
生成の入口は 1 つです。

```bash
npm run build          # データ検証 → 各種生成 → 件数 → sitemap → dist/
git diff --exit-code   # 生成物が最新かを確かめる
```

CI も同じコマンドを流し、差分が出たら落とします。**CI からリポジトリへ
push しません。**直すのは手元です。

データを変えたときは、**`npm run build` を差分が出なくなるまで流してください**
（通常は 2 回）。科目トップの更新日は「その HTML の中身が変わった日」なので、
1 回目で科目トップが書き換わると、2 回目でそれを読む一覧ページの更新日が動きます。
2 回目で必ず収まります。収まらなければ、生成が入力に依存していない箇所があるので
報告してください。

## 検証コマンド

```bash
npm ci
npm run check:data       # ISBN・id・確認状態（生成の前の関門）
npm test                 # ユニットテスト
npm run check:site       # 文章・孤立ページ・リンク・誇張語
npm run build            # 生成
git diff --exit-code     # 生成物が最新か
npm run test:e2e         # E2E とアクセシビリティ（320/375/768/1366px）
npm run check:counts     # 収録冊数
```

E2E は **HTTP 経由**で走ります（`build/serve.mjs`）。`file://` で開いた確認は
本番と挙動が違うので、確かめたことになりません。

## 書いてはいけないこと

- 現物を確認していないのに、紙面の構成・問題の質・使用感を体験談として書く
- 確認していない書誌情報・難易度・学習時間を断定する
- 合格保証、絶対評価、模試間の固定換算
- 大学・出版社・予備校との公式な関係を示唆する表現
- 架空の監修者・専門家レビュー・現物確認済み表示
- 誇張語（`docs/style-guide.md` の 2 節。検査で落ちます）

## 診断の選択肢を足すとき

共有 URL は選択肢の**並び順**（1 始まりの番号）で回答を持ちます。
**必ず末尾に足してください。**前や間に挿すと、過去に共有された URL が
別の回答として復元されます。`test/share.test.mjs` の指紋と
`test/route-reach.test.mjs` の fixture が変更を検出します。

## 解析イベントを足すとき

`assets/js/analytics.js` の allowlist と `docs/analytics-events.md` の両方を
直します。片方だけだと `test/analytics.test.mjs` が落ちます。
**志望校・学部・偏差値・回答・既習教材は送りません。**

## ライセンス

このリポジトリにライセンスは指定されていません（2026-09 時点）。
追加は権利者の判断が要るため、こちらでは決めません。
