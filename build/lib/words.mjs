/**
 * 文章の検査に使う語のリスト。**`docs/style-guide.md` 2 節と同じものを持つ。**
 *
 * ここと条文が食い違うと「書いてあるのに守られない規則」ができる。片方だけ直せない
 * ように、`test/style-guide.test.mjs` が条文の 2 節を読んでこのファイルと突き合わせる。
 * 語を足すときは条文とここの両方を直す（テストがどちらかを忘れた状態で落ちる）。
 *
 * 使うのは `build/check-site.mjs`。ここに置いているのは、条文との突き合わせを
 * `check-site.mjs` 全体を動かさずにテストから読めるようにするため。
 */

/** 禁止語（誇張・情緒）のうち、部分一致で検出できるもの */
export const BANNED_WORDS = [
  '伝説', '名著', '最終兵器', '君臨', '滅法', '豪語', '最右翼', '国民的', '金字塔',
  '決定版', '最強', '圧倒的', '無双', '必殺', '鉄板', '至高', '究極', '王者', '絶対',
];

/**
 * 禁止語のうち、機械では検出しないもの。
 *
 * 「神」は神戸大・神経・昼神（人名）のように地名・人名・語の一部として実在し、
 * 部分一致では誤検出しか出ない。条文の禁止語ではあり続けるが、検査は人の目に任せる。
 * 除外の理由をここに残さないと、次に見た人が「消し忘れ」と判断して足してしまう。
 */
export const BANNED_NOT_CHECKED = ['神'];

/** サイトの呼び方。Web サイトなので「アプリ」と書かない */
export const BANNED_PHRASES = ['本アプリ', '当アプリ'];

/**
 * 誇張語の検査から外す固有名詞。
 *
 * `docs/style-guide.md` 2 節の「例外」— 書名・シリーズ名にこれらの語が入っている本は
 * そのまま書く — のうち、**収録している本の書名リストに載らないもの**だけを置く。
 * 書名・正式名称は `check-site.mjs` が `BOOKS` から自動で集めるので、ここには要らない。
 *
 * 足してよいのは「出版社が付けたシリーズ名で、どの本の name / official にも現れないもの」
 * に限る。地の文の誇張を通すための逃げ道にしない。
 */
export const BANNED_ALLOW = [
  '究極の東大対策', // 東進ブックスのシリーズ名。english/todai-eigo-sokogi の problems が引用する
];
